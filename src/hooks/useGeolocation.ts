/**
 * Hook de géolocalisation simple - DÉPRÉCIÉ
 * @deprecated Utiliser useSmartGeolocation pour un système unifié et professionnel
 */

import { useSmartGeolocation } from './useSmartGeolocation';

// Alias de compatibilité - redirige vers le système unifié professionnel
export const useGeolocation = useSmartGeolocation;